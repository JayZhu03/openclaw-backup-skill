#!/bin/bash
SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ "$OSTYPE" == "msys" || "$OSTYPE" == "win32" ]]; then
    powershell.exe -ExecutionPolicy Bypass -File "$SKILL_DIR/scripts/windows/restore.ps1" "$@"
else
    bash "$SKILL_DIR/scripts/unix/restore.sh" "$@"
fi
